data_list = ['load data', 'export data', 'analyze & predict']
choice = input('Select an operation to make')
while not choice:
    choice = input('Select an operation to make')
    # try:
    choice = int(choice)
    # except:

print(choice)
